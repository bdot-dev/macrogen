var CodeList = (function($) {
	var options;
	var init = function(_options) {
		console.log('Login:', _options);
		var options = _options;

		// bind event
		
		// load시 처리할 것

	};
	
	return {
		init : init
	};
	
}(jQuery));